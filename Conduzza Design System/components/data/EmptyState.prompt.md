Zero state. Say what's missing, then offer the one action that fixes it. Never apologise, never use an illustration.

```jsx
<EmptyState icon="calendar-x" title="Ninguém na lista de espera" description="Pacientes sem horário adequado aparecem aqui automaticamente." action={<Button variant="secondary" icon="plus">Adicionar manualmente</Button>} />
```