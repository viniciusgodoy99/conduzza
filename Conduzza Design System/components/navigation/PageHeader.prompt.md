Section header inside the page body — title, one explanatory line, actions on the right.

```jsx
<PageHeader eyebrow="HOJE · 22 SET" title="Confirmações" description="147 consultas agendadas para hoje." actions={<Button variant="primary" icon="send">Disparar lembretes</Button>} />
```