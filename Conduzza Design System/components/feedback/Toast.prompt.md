Transient confirmation, bottom-right, ~4s. Always dark so it reads over any page.

```jsx
<Toast title="Lembretes disparados" description="128 pacientes notificados." />
```

One toast at a time. If the user must act on it, use `Banner` instead.