Pill search. One global instance in the top bar (with `shortcut="⌘K"`) and one contextual instance per list.

```jsx
<SearchField placeholder="Buscar conversas, pacientes, telefones…" shortcut="⌘K" />
<SearchField size="sm" placeholder="Filtrar leads" />
```

Sunken by default; it lifts to white on focus.