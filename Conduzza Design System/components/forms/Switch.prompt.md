Turns a behaviour on or off, applied immediately (no Save button). Every automation and permission row is a Switch.

```jsx
<Switch checked={on} onChange={setOn} label="Confirmação de consulta" description="Dispara 24h antes do horário marcado" />
```

Use `Checkbox` when the choice is part of a form that gets submitted.